package br.com.eng.laje;

import java.io.IOException;

import br.com.eng.entities.Coeficientes;
import br.com.eng.entities.LajeComParede;
import br.com.eng.entities.LajeSemParede;
import br.com.eng.entities.Materiais;
import br.com.eng.entities.Parede;
import javafx.fxml.FXML;

public class SecondaryController {
	
	PrimaryController primaryController;
	
	private LajeComParede lajeComParede = primaryController.getLajeComParede();
	
	private LajeSemParede lajeSemParede = primaryController.getLajeSemParede();
	
	private Parede parede = primaryController.getParede();

	private Materiais materiais = primaryController.getMateriais();

	private Coeficientes coeficientes = primaryController.getCoeficientes();
	
	private Coeficientes coeficientesMxKx = primaryController.getCoeficientesMxKx();

	@FXML
	private void btRetornaTelaDeCalculos() throws IOException {
		
		App.setRoot("primary");
		
	}
	
	
	
}